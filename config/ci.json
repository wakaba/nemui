{
  "github": {
    "build": [
      "make deps build-for-docker",
      {"docker_build": "quay.io/wakaba/nemui"}
    ],
    "tests": {"deploy": [
      {"docker_push": "quay.io/wakaba/nemui", "branch": "heroku"}
    ]},
    "pages": {"branch": "heroku"}
  }
}
