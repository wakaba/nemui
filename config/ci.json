{
  "github": {
    "build": [
      "make deps build-for-docker",
      {"docker_build": "quay.io/wakaba/sandbox:kuzu"}
    ],
    "tests": {"deploy": [
      {"docker_push": "quay.io/wakaba/sandbox:kuzu"}
    ]},
    "pages": {"branch": "heroku"}
  }
}
