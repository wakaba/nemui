{
  "github": {
    "build": [
      "make deps build-for-docker",
      {"docker_build": "quay.io/wakaba/sandbox"}
    ],
    "tests": {"deploy": [
      {"docker_push": "quay.io/wakaba/sandbox", "branch": "heroku"}
    ]},
    "pages": {"branch": "heroku"}
  }
}
