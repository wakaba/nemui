{
  "github": {
    "build": [
      "make build-for-docker",
      {"docker_build": "$DDSDRUN_DOCKER_IMAGE"}
    ],
    "tests": [
      {"docker_push": "$DDSDRUN_DOCKER_IMAGE", "branch": "ddsdrun"}
    ]
  }
}
