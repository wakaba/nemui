{
   "kind" : "pipeline",
   "name" : "default",
   "steps" : [
      {
         "commands" : [
            "cd \u005C/app && perl local/bin/pmbp.pl --install-commands docker",
            "pwd",
            "pwd",
            "make test-deps",
            "make test",
            "ls",
            "docker version"
         ],
         "image" : "quay.io/wakaba/docker-perl-app-base",
         "name" : "build",
         "volumes" : [
            {
               "name" : "dockersock",
               "path" : "/var/run/docker.sock"
            }
         ]
      }
   ],
   "type" : "docker",
   "volumes" : [
      {
         "name" : "dockersock",
         "path" : "/var/run/docker.sock"
      }
   ]
}
